import logo from './logo.svg';
import './App.css';

function Button(props) {
  
  return (
    <div className="App">
      
      <button>{props.title}</button>
      <button>add21</button>
    </div>
  );
}

export default Button;
